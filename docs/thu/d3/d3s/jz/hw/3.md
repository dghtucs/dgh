1. D
SSD：读/写是按 page，擦除才是按 block

2. D   非向量模式下发生异常/中断时，pc 跳转到 mtvec

3. C   `mret` 不会“清除异常原因”如 `mcause`

4. A   处理中断期间不被其他中断打断：通常做法是全局关中断

5. 空间利用率 (4−1)/4 = 75%，比 RAID1 更高

6. 5 年 1DWPD：总全盘写入次数 = (5 \times 365 = 1825) 天 ⇒ 每个闪存块最少擦除 1825 次
